#include "version.xcconfig"

// User-defined: Release plist omits OSLogPreferences (same keys otherwise as Topaz/Info.plist).
TOPAZ_APP_INFOPLIST = Info-Release.plist
INFOPLIST_FILE = Topaz/$(TOPAZ_APP_INFOPLIST)

CODE_SIGN_IDENTITY = Apple Distribution
CODE_SIGN_STYLE = Manual
PROVISIONING_PROFILE_SPECIFIER = Topaz Distribution
